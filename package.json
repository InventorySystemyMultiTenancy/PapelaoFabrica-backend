{
  "name": "papelao-fabrica-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install --include=dev && npm run build",
    "start": "cd backend && npm run start"
  },
  "engines": {
    "node": ">=18.17"
  }
}
